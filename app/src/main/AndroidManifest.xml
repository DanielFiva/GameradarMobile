<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="GameRadar"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameRadarMobile">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CatalogActivity"
            android:label="Catálogo" />
        <activity android:name=".CreateUserActivity" />
        <activity android:name=".GameDetailsActivity"
            android:label="Detalles Juego" />
        <activity android:name=".ChatActivity"
            android:label="Chat" />
        <activity android:name=".GameReviewsActivity"
            android:label="Reviews" />
        <activity android:name=".WriteReviewActivity"
            android:label="Write review" />
    </application>

</manifest>